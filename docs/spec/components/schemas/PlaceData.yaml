type: object
required:
  - id
  - type
  - attributes
  - relationships
properties:
  id:
    type: string
    format: uuid
    description: "place id"
  type:
    type: string
    enum: [ place ]
  attributes:
    $ref: './PlaceAttributes.yaml'
  relationships:
    $ref: './PlaceRelationships.yaml'